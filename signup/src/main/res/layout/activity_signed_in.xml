<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                             xmlns:app="http://schemas.android.com/apk/res-auto"
                                             xmlns:tools="http://schemas.android.com/tools"
                                             android:id="@+id/relativeLayout"
                                             android:layout_width="match_parent"
                                             android:layout_height="match_parent"
                                             android:padding="16dp"
                                             tools:context=".SignedInActivity">

    <ProgressBar
            style="?android:attr/progressBarStyle"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:id="@+id/progress_load_users"
            app:layout_constraintEnd_toEndOf="@+id/user_list_view"
            app:layout_constraintTop_toTopOf="@+id/user_list_view"
            app:layout_constraintBottom_toBottomOf="@+id/user_list_view"
            app:layout_constraintStart_toStartOf="@+id/user_list_view"
            android:visibility="visible"/>
    <Button
            android:id="@+id/users"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:text="Users"
            app:layout_constraintEnd_toStartOf="@+id/groups"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

    <Button
            android:id="@+id/groups"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:text="Groups"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toEndOf="@+id/users"
            app:layout_constraintTop_toTopOf="@+id/users"/>

    <ListView
            android:id="@+id/user_list_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="1.0"
            app:layout_constraintStart_toEndOf="@+id/users"
            app:layout_constraintTop_toBottomOf="@+id/groups"
            app:layout_constraintVertical_bias="1.0">
    </ListView>

</android.support.constraint.ConstraintLayout>